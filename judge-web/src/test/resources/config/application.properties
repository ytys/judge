spring.profiles.active = test
spring.profiles.include = prod

# datasource for test purpose, usually disable to use h2 instead.
spring.datasource.url = jdbc:mariadb:///oj_test_db?zeroDateTimeBehavior=convertToNull&characterEncoding=UTF-8
spring.datasource.username = root
spring.datasource.password = root

